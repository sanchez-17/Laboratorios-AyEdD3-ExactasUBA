#include "../../backtracking/utilities.h"
int N;vector<int> numbers;
//Estrategia greedy: Elegir la actividad que termine antes.
void solve(vector<int>& numbers,int i){
	|||}

int main(){
	cin>>N;numbers.rz(N);
	forn(i,N) cin >> numbers[i];
	sort(numbers.begin(),numbers.end());
	solve(numbers);
	return 0;
}

